# 삼성 SW 역량테스트 커리큘럼

**목표**: 삼성 코테 데뷔 · **현재**: 백준 골드1 경험 · **기간**: 8주 (주 5문제 기준 약 40문제)

> **백준(BOJ)은 2026-04-28 서비스 종료**했다. 채점 서비스와 함께 복귀 예정이라고는 하나 시점이 불명확해
> 이 커리큘럼에서 제외했다. `problems/baekjoon/` 폴더는 복귀에 대비해 남겨둔다.

## 삼성 코테는 뭐가 다른가

| | 일반 코테 (카카오 등) | 삼성 SW 역량테스트 |
|---|---|---|
| 문제 수 / 시간 | 5~7문제 / 5시간 | **2문제 / 3시간** |
| 채점 | 부분 점수 있음 | **전부 통과 아니면 0점** |
| 출제 유형 | 자료구조·알고리즘 폭넓게 | **시뮬레이션 · 완전탐색 · BFS 에 집중** |
| 요구 능력 | 알고리즘 선택 | **긴 요구사항을 틀리지 않고 구현하는 지구력** |

골드1을 풀어봤다면 **알고리즘 지식은 이미 충분하다.** 부족한 건 딱 두 가지다.

1. **구현 지구력** — 요구사항이 A4 한 장이고 단계가 5~6개인 문제를 한 번에 맞히는 능력
2. **디버깅 속도** — 부분 점수가 없어서, 틀린 원인을 20분 안에 찾아야 한다

새 알고리즘을 배우는 게 아니라 **기출 시뮬레이션을 반복**하는 구성이다.

## 사이트 분담

| 사이트 | 역할 | 비용 |
|---|---|---|
| **SWEA** | **1순위.** 삼성이 직접 운영. `모의 SW 역량테스트` 문제셋이 곧 기출이다 | 무료 |
| **코드트리** | **2순위.** 삼성 기출을 난이도순 트랙으로 정리해둠. 해설이 붙어 있다 | 유료 |
| 프로그래머스 | 워밍업 · 완전탐색/BFS 기본기 | 무료 |
| LeetCode | 삼성 대비로는 **안 맞는다.** 시뮬레이션 유형이 거의 없다 | — |

문제를 어디서 풀든 **환경은 SWEA 에 맞춘다** — 삼성 본시험이 SWEA 기반이다.

---

## Phase 0 — 감 되찾기 (1주)

방향벡터 + 완전탐색 + BFS 뼈대가 몸에 붙어야 한다. 프로그래머스로 손부터 푼다.

- [ ] PG `42839` 소수 찾기 — 순열 완전탐색
- [ ] PG `42842` 카펫 — 완전탐색
- [ ] PG `43165` 타겟 넘버 — DFS
- [ ] PG `43162` 네트워크 — 연결 요소
- [ ] PG `1844` 게임 맵 최단거리 — 격자 BFS
- [ ] SWEA `1954` 달팽이 숫자 — 격자 이동
- [ ] SWEA `2001` 파리 퇴치 — 부분 격자 탐색

**목표**: `dx/dy` 배열, 백트래킹 뼈대, BFS 뼈대를 보지 않고 친다.

## Phase 1 — SWEA 모의 SW 역량테스트 입문 (2주)

SWEA `학습 > 모의 SW 역량테스트` 에서 난이도 낮은 순으로.

- [ ] SWEA `4008` 숫자 만들기 — 연산자 순열
- [ ] SWEA `4012` 요리사 — 조합
- [ ] SWEA `1954`~ 이후 격자 워밍업 마무리
- [ ] SWEA `2001` 이후 부분탐색 마무리
- [ ] SWEA `4013` 특이한 자석 — 연쇄 회전 (톱니바퀴 유형)
- [ ] SWEA `1767` 프로세서 연결하기 — 백트래킹 + 가지치기
- [ ] SWEA `2819` 격자판의 숫자 이어 붙이기 — DFS + 중복 제거
- [ ] SWEA `1949` 등산로 조성 — DFS + 상태 복원
- [ ] SWEA `2117` 홈 방범 서비스 — 완전탐색 + 거리
- [ ] SWEA `2115` 벌꿀 채취 — 조합 + 부분집합

**목표**: 한 문제 60분 안에 구현 완료. 요구사항을 단계별 함수로 쪼개는 습관.

## Phase 2 — SWEA 모의 SW 역량테스트 중급 (2주)

여기부터가 실제 삼성 난이도다.

- [ ] SWEA `2382` 미생물 격리 — 동시 이동 + 충돌 처리 (**반드시 푼다**)
- [ ] SWEA `5656` 벽돌 깨기 — 완전탐색 + 연쇄 파괴
- [ ] SWEA `5658` 보물상자 비밀번호 — 회전 + 중복 제거
- [ ] SWEA `5648` 원자 소멸 시뮬레이션 — 동시 이동 + 좌표 스케일링
- [ ] SWEA `2105` 디저트 카페 — 격자 순회 완전탐색
- [ ] SWEA `2112` 보호 필름 — 백트래킹 + 조기 종료
- [ ] SWEA `1953` 탈주범 검거 — 방향 제한 BFS
- [ ] SWEA `2383` 점심 식사시간 — 조합 + 시간 시뮬레이션
- [ ] SWEA `2477` 차량 정비소 — 다중 큐 시뮬레이션
- [ ] SWEA `5653` 줄기세포 배양 — 상태 전이 + 좌표 확장

**목표**: 구현 전에 복잡도를 미리 계산하는 습관. 시간 초과를 코드 쓰기 전에 잡는다.

## Phase 3 — 코드트리 삼성 기출 트랙 (2주)

코드트리의 **삼성 SW 역량테스트 기출** 코스를 난이도순으로 훑는다.
SWEA 에 없는 최신 기출(상어 시리즈, 마법사 상어 계열 등)이 여기 정리돼 있고 해설이 붙는다.

- [ ] 코드트리 삼성 기출 — 시뮬레이션 트랙 완주
- [ ] 코드트리 삼성 기출 — 완전탐색 트랙 완주
- [ ] 코드트리 삼성 기출 — BFS/DFS 트랙 완주
- [ ] 틀린 문제만 모아 2회독

SWEA `5644` 무선 충전, `5650` 핀볼 게임, `4014` 활주로 건설도 이 시기에 같이 푼다.

**목표**: 요구사항 → 함수 분해 → 구현을 90분 안에.

## Phase 4 — 실전 모의 (1주)

**타이머 켜고 3시간에 2문제.** 못 푼 문제는 다음 날 복기한다.

- [ ] 모의 1 — 안 푼 SWEA 모의 역량테스트 문제 2개
- [ ] 모의 2 — 〃
- [ ] 모의 3 — 코드트리 실전 모의고사
- [ ] 오답 노트 정리 (틀린 **이유**만 모아서 `concepts/` 에)

---

## 삼성 문제에서 실제로 틀리는 이유

거의 이 다섯 가지다. 풀 때마다 의식적으로 점검해라.

1. **깊은 복사를 안 함** — 격자를 복사한 줄 알았는데 참조만 복사
   - Java: `arr.clone()` 은 2차원에서 **얕은 복사다.** 행마다 복사해야 한다
   - C++: `vector<vector<int>>` 는 대입이 값 복사 → 안전하지만 느릴 수 있다
2. **동시 이동을 순차로 처리** — "모두 동시에 이동" 인데 하나씩 옮겨서 결과가 달라짐. 새 배열에 쓰고 마지막에 교체
3. **방향 배열 순서** — 문제가 정한 우선순위(예: 위 → 왼쪽 → 아래 → 오른쪽)를 임의로 바꿈
4. **오버플로우** — 곱셈이 나오면 `long` / `long long`
5. **종료 조건 누락** — "더 이상 변화가 없으면 종료" 를 빼먹고 무한 루프

## 풀기 전 30초 체크

- 격자 크기 N, 시간 T → **최악 연산 수**를 먼저 계산한다. `N⁴ × T` 가 1억 넘으면 다시 생각
- 요구사항의 **단계**를 번호로 적는다. 그대로 함수 이름이 된다 (`move()`, `merge()`, `spread()`)
- 예제가 2개 이상이면 **둘 다** 손으로 돌려본다. 1개만 맞고 2개째 틀리는 게 제일 흔하다
- SWEA 는 **테스트케이스 루프**(`for tc in 1..T`)가 기본이다. 전역 상태를 매 케이스마다 초기화했는지 확인

## 언어 전략

**본선은 Java 로 친다.** C++ 는 Phase 2 까지만 병행하고, Phase 3 부터는 Java 에 집중해라.
두 언어를 끝까지 병행하면 문제 수가 반으로 준다 — 삼성은 푼 문제 수가 곧 실력이다.

| | Java | C++ |
|---|---|---|
| 격자 복사 | `for (int i=0;i<n;i++) b[i]=a[i].clone();` | `b = a;` |
| 덱 | `ArrayDeque<int[]>` | `deque<array<int,2>>` |
| 우선순위 큐 | `PriorityQueue<>(Comparator.comparingInt(...))` | `priority_queue<T, vector<T>, greater<>>` |
| 입력 | `BufferedReader` + `StringTokenizer` | `cin` + `sync_with_stdio(false)` |

## 진행 방법

```bash
cp -r ../_template-problem swea/2382
```

풀고 나서 `/feedback problems/swea/2382` → 복잡도·문제점·대안이 채워진다.
Phase 가 끝날 때마다 위 체크박스를 채우고 커밋한다.

```bash
git cm  →  d  →  "Phase 0 완료"
```

> SWEA 문제 번호는 사이트 개편으로 바뀔 수 있다. 제목으로 검색해서 확인하고 폴더를 만들어라.
