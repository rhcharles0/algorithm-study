#include <bits/stdc++.h>
using namespace std;

// 프로그래머스류: 이 함수만 제출
int solve(vector<int> arr) {
    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 백준/SWEA류 표준입력 파싱은 여기서

    // 예제 케이스 검증
    assert(solve({1, 2, 3}) == 0);
    cout << solve({1, 2, 3}) << "\n";
}
